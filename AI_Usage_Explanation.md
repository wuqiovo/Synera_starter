# AI_Usage_Explanation

## 摘要
本次实现围绕阶段一需求完成了玩家/敌方信息展示、备战区可视化与拖拽、单位血量显示、敌方总血量与回合结算逻辑、结束部署按钮与回合状态管理，并保持原有棋盘拖拽逻辑不变。通过引入信号-槽更新机制，界面刷新与游戏逻辑解耦，便于后续阶段二/三扩展（战斗流程、经济与商店等）。

## 对照 checklist 的功能覆盖
### 阶段一：基础棋盘与单位交互
- 实现棋盘与半场划分：沿用 Board 的上下半区规则，并在 GUI 上区分颜色。
- 实现备战区：新增备战区槽位绘制与拖拽落位逻辑。
- 实现 Unit 基类与统一 Unit：保持统一 Unit 类型并通过 owner 区分敌我。
- 实现玩家实体与敌方轮次生成：回合推进时生成敌方单位并落位到上半区。
- 支持拖拽摆放与非法放置处理：棋盘内拖拽沿用旧逻辑，新增棋盘与备战区双向拖拽，并做合法性校验。
- GUI 展示：棋盘/备战区/单位信息/玩家与敌方信息面板均已显示。

### 阶段二：战斗流程与自动对战
- 目前仅提供“结束部署”触发回合结算的雏形，不含完整战斗状态机与寻路。

### 阶段三：经济、羁绊、升星、装备与存档
- 预留商店区域空间，尚未实现经济与商店逻辑。

### 阶段四：扩展功能
- 未实现（保持扩展位）。

## 核心函数说明
### Game::reset()
通过清空棋盘与备战区、重置回合与敌方总血量、重新创建初始玩家单位并调用 startNextRound() 生成敌方单位，完成“初始状态重置与开局敌人生成”。优点是逻辑集中、状态一致，在回合初始化与界面刷新方面更稳定。

### Game::startNextStage()
通过推进 stage 并调用 reset() 复用重置流程，完成“关卡推进与状态重建”。优点是复用初始化逻辑，减少重复代码。

### Game::startNextRound()
通过递增 round、调用 spawnEnemiesForCurrentStage() 并同步场景，完成“回合推进与敌方生成”。优点是逻辑清晰，在敌人生成与界面同步方面更可控。

### Game::endDeployment()
通过切换战斗状态并调用 resolveRoundFromCurrentBoard()，完成“部署结束后的回合结算触发”。优点是流程显式，便于后续接入完整战斗阶段。

### Game::handleDragStarted()
通过识别拖拽来源（棋盘/备战区），记录拖拽状态并提升图元层级，完成“拖拽开始管理”。优点是兼容原棋盘拖拽逻辑并扩展备战区拖拽。

### Game::handleDragMoved()
通过对棋盘格与备战区槽位做高亮判定，完成“拖拽过程中的目标提示”。优点是交互反馈更明确，降低非法摆放概率。

### Game::handleDropCommand()
通过区分落点类型（棋盘/备战区）并选择对应搬运逻辑，完成“拖拽落点最终提交”。优点是流程统一，兼容棋盘内移动与棋盘-备战区互换。

### Game::spawnEnemiesForCurrentStage()
通过创建 3 个敌方单位并优先放置到上半区预设坐标，完成“敌方轮次生成”。优点是易于扩展为按关卡配置生成。

### Game::buildScene()
通过构建棋盘格子图元、备战区槽位图元、单位图元并设置场景范围，完成“场景可视化搭建”。优点是集中式渲染初始化，便于调整布局与视觉层级。

### Game::syncFromBoard()
通过读取棋盘与备战区数据更新单位图元位置与可见性，完成“逻辑到视图同步”。优点是将显示状态与数据源保持一致。

### Game::resolveRoundFromCurrentBoard()
通过统计存活单位数并触发 applyRoundDamage()，完成“回合结算与总血量扣减”。优点是与规则一致，便于后续接入完整战斗模拟。

### Game::applyRoundDamage()
通过按剩余单位数计算伤害并更新玩家/敌方总血量，完成“回合伤害结算”。优点是规则集中，方便调整伤害公式。

### GameWindow::setupUI()
通过搭建顶部信息区、中央棋盘区与底部按钮区并连接信号槽，完成“GUI 架构初始化”。优点是职责明确，布局可扩展（商店区域预留）。

### GameWindow::refreshInfoPanels()
通过读取 Game 的玩家/敌方数据并更新 QLabel，完成“信息面板刷新”。优点是显示逻辑集中，减少重复更新。

### GameWindow::onBattleStateChanged()
通过根据战斗状态禁用/启用结束部署按钮，完成“回合状态下的交互控制”。优点是避免战斗中误操作。

### UnitItem::paint()
通过绘制单位头像与血量文本，完成“单位头顶血量显示”。优点是信息直观，满足阶段一展示要求。
