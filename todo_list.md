# 阶段一 TODO 清单（实现内容 + 方向）

## 0. 目标范围
- 目标：完成阶段一的基础玩法闭环（棋盘、备战区、单位、拖拽摆放、基础数据同步）。
- 说明：本清单优先覆盖逻辑层与数据层，GUI 细节只保留必要联调点。

## 1. 棋盘与半场规则
- [x] 完成棋盘地块占用规则（单格仅允许 1 个单位）。
- [x] 明确半场划分规则（玩家下半区、敌方上半区）。
- [x] 提供统一接口：坐标合法性校验、地块查询、占用查询、添加/移除单位。

实现方向：
- 建议保留 Board 作为唯一“地块真值来源”。
- 所有落位和移动先走合法性校验，再修改状态，最后广播/同步。
- 先保证规则正确，再做性能优化。

## 2. 备战区 Bench
- [x] 新建 Bench 数据结构（建议固定 8 格）。
- [x] 支持 Bench 与棋盘之间的双向放置。
- [x] 处理非法放置（回弹原位或交换规则）。

实现方向：
- Bench 可设计为与 Board 类似的槽位容器，接口保持风格一致。
- Game 只做流程协调：判定来源区域 -> 判定目标区域 -> 调用 Board/Bench 写入。
- 保证一次操作的原子性，避免“从来源删了但目标没放上”的中间态。

## 3. Unit 基类与敌我统一类型
- [x] 确认 Unit 包含阶段一必要字段：HP、ATK、Range、MaxMana、Mana。
- [x] 使用 owner 字段区分敌我，不拆分 PlayerUnit/EnemyUnit 两套类型。
- [x] 确保单位有唯一 ID，便于拖拽、查找、同步。

实现方向：
- Unit 保持轻量数据模型，不耦合 UI 逻辑。
- 若后续要扩展技能/羁绊，可在 Unit 上增加标签字段与可扩展属性结构。

## 4. 玩家实体与敌方轮次生成
- [x] 新建 Player 数据对象：血量、金币、等级、人口上限等。
- [x] 新建敌方轮次生成器（按 round 生成敌方阵容）。
- [ ] 约定敌方出生区域与初始落点规则。

实现方向：
- Player 建议独立成类，避免把经济/血量直接塞进 Game。
- 敌方生成先做最简版本：按轮次表生成固定模板，再逐步参数化。

## 5. 拖拽摆放与非法放置
- [ ] 完成拖拽开始、拖拽中、拖拽释放三段流程。
- [ ] 放置前校验来源与目标是否合法。
- [ ] 非法放置时恢复原位，不污染数据。

实现方向：
- 统一 drop 校验函数，集中处理：坐标合法、半场规则、目标是否占用、来源是否匹配。
- UI 只负责反馈状态，最终结果以逻辑层校验为准。

## 6. 数据同步（Board/Bench -> 显示层）
- [ ] 每次状态变更后触发一次统一同步。
- [ ] 同步包括：位置、显隐、所属区域（棋盘或备战区）。
- [ ] 避免在多个地方重复写 UI 状态，减少不同步问题。

实现方向：
- 采用单向同步：逻辑层改数据 -> 调用 sync 函数刷新显示。
- 后续可升级为事件驱动（信号槽）减少手动调用。

## 7. 阶段一验收检查
- [ ] 能创建并显示基础棋盘。
- [ ] 能创建并显示初始单位。
- [ ] 能在合法区域拖拽移动单位。
- [ ] 非法放置会回退或按规则处理。
- [ ] Board 与 Bench 数据与显示一致。
- [ ] 敌我可通过同一 Unit 类型 + owner 区分。

---

## 推荐实现顺序（最小可交付）
1. 先封闭 Board 与 Bench 的数据接口。
2. 再打通 Game 的拖拽校验与落位提交。
3. 接入统一 sync，保证每次操作后画面一致。
4. 最后补 Player 与敌方轮次生成的最简版本。

## 代码结构建议
- core/
  - board.h/.cpp
  - bench.h/.cpp（新增）
  - game.h/.cpp
  - player.h/.cpp（新增）
  - enemy_wave_generator.h/.cpp（新增）
- entity/
  - unit.h/.cpp

## 风险提示
- 最大风险是“状态分散”：同一单位位置在多个容器里不一致。
- 规避方式：统一真值来源，所有改动通过单一流程入口执行。

---

# 阶段二 TODO 清单（非 GUI）

## 1. 战斗流程主循环
- [ ] 新增阶段状态机：准备阶段 -> 战斗阶段 -> 结算阶段。
- [ ] 增加轮次推进与阶段切换触发条件（时间/战斗结束）。
- [ ] 在 Game 或独立 BattleSystem 中实现每帧/tick 更新入口。

实现方向：
- 建议新增 `core/battle_system.h/.cpp`，避免把战斗逻辑堆到 Game。
- 先做最小可运行循环，再补完整规则细节。

## 2. 敌方生成与轮次增强
- [ ] 实现按 round 生成敌方阵容（数量、属性、种类可配置）。
- [ ] 明确敌方出生点与初始站位规则（上半区）。
- [ ] 每轮提升敌方强度（至少满足数量或属性增长）。

实现方向：
- 建议新增 `core/enemy_wave_generator.h/.cpp`。
- 先用静态波次表，后续再做随机池或权重配置。

## 3. 单位状态机
- [ ] 在 Unit 或战斗组件中加入状态：Idle/Moving/Attacking/Casting/Dead。
- [ ] 明确状态切换条件（目标丢失、射程满足、满蓝施法、死亡）。
- [ ] 死亡单位从棋盘/容器中正确移除。

实现方向：
- 保持状态切换在逻辑层统一处理，避免 UI 驱动逻辑。

## 4. 索敌规则
- [ ] 实现“欧氏距离最近”选敌。
- [ ] 距离相同按规则决策：生命值高低 -> 从左到右 -> 从下到上。
- [ ] 无可攻击目标时进入移动逻辑。

实现方向：
- 建议封装 `findTarget(unit, enemies)` 纯函数，便于单测。

## 5. 寻路、阻挡与碰撞
- [ ] 实现基础路径搜索（BFS/A* 任选其一）。
- [ ] 移动不可穿过单位，占用冲突时等待或重算路径。
- [ ] 保证同一地块始终仅 1 单位。

实现方向：
- 优先实现四/六邻接一致规则下的最短路径。
- 先保证正确性，再优化性能与缓存。

## 6. 普攻、回蓝、技能与胜负结算
- [ ] 普攻伤害结算与攻击间隔。
- [ ] 每次普攻回复 10 法力。
- [ ] 满蓝后自动施法并清空法力。
- [ ] 至少实现 3-5 个不同技能（如眩晕/AOE/回血）。
- [ ] 实现胜负结算：一方全灭结束；玩家血量为 0 则失败。

实现方向：
- 建议技能走多态或策略模式：`Skill::cast(caster, context)`。
- 胜负结算统一在 BattleSystem 收口，避免分散判断。

---

# 阶段三 TODO 清单（非 GUI）

## 1. 经济与商店
- [ ] 完善 Player 实体（`player.h` 目前为空）：血量、金币、等级、人口上限。
- [ ] 实现关卡奖励金币发放。
- [ ] 实现商店 5 选随机刷新池。
- [ ] 实现购买与刷新消耗。

实现方向：
- 建议新增 `core/shop.h/.cpp` 维护卡池与刷新逻辑。
- 金币扣减与校验统一放在 Player/经济模块接口中。

## 2. 人口上限与上阵限制
- [ ] 实现升级人口（消耗金币，每级 +1 上限）。
- [ ] 校验棋盘上阵数量不得超过人口上限。
- [ ] 超限时禁止上阵并返回明确失败原因。

实现方向：
- 在 board/bench 转移入口统一做人口检查，避免绕过。

## 3. 羁绊系统
- [ ] 设计并落地 4-6 种羁绊。
- [ ] 至少 2 种属性光环 + 1 种机制改变。
- [ ] 基于单位 traits 统计生效阈值并应用增益。

实现方向：
- `Unit::traits` 已存在，可新增 `core/synergy_system.h/.cpp` 计算全局加成。

## 4. 升星系统
- [ ] 实现 3 合 1（同名同星）自动合成。
- [ ] 增加星级字段与 2 星属性提升（建议 1.5-2 倍）。
- [ ] 合成后更新棋盘/备战区数据一致性。

实现方向：
- 建议在购买入库、回合结算后触发一次合成检测。

## 5. 装备系统
- [ ] 实现装备掉落与背包存储。
- [ ] 实现单位穿戴限制（1 星 1 件，2 星可扩展 2 件）。
- [ ] 实现至少 4 种基础装备效果：铁剑/锁子甲/急速手套/蓝水晶。

实现方向：
- 建议新增 `entity/equipment.h` 与 `core/equipment_system.h/.cpp`。
- 装备效果统一在战斗计算链生效（攻击、血量、攻速、法力上限）。

## 6. 存档与读档
- [ ] 实现序列化/反序列化（建议 JSON 起步）。
- [ ] 保存并恢复：玩家状态、轮次、棋盘、备战区、单位属性、装备。
- [ ] 读档后保证可继续对局且状态一致。

实现方向：
- 建议新增 `core/save_load.h/.cpp`，集中处理版本号与兼容字段。

---

# 阶段一到三 GUI 完善 TODO（常规内容之后）

## 阶段一 GUI
- [ ] 展示棋盘与备战区网格（含玩家/敌方半场视觉区分）。
- [ ] 展示单位基础信息（生命/法力/关键属性）。
- [ ] 完成 board <-> bench 拖拽交互与非法放置反馈（高亮/回弹/交换提示）。

## 阶段二 GUI
- [ ] 展示当前阶段（准备/战斗/结算）与轮次编号。
- [ ] 展示单位状态（Idle/Moving/Attacking/Casting/Dead）与目标反馈。
- [ ] 展示攻击与施法表现（最小可见反馈即可，如闪烁/文字/特效占位）。
- [ ] 展示战斗结果与回合结算面板。

## 阶段三 GUI
- [ ] 展示玩家面板：血量、金币、等级、人口（已上阵/上限）。
- [ ] 展示商店 5 选、购买按钮、刷新按钮与花费。
- [ ] 展示羁绊面板（激活数量与效果说明）。
- [ ] 展示星级、装备栏与装备属性变更。
- [ ] 提供存档/读档入口与成功/失败提示。
